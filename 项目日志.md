# My_quadruped_robot_lab项目日志

本日志用于记录项目开发过程中遇到的技术问题以及对应的解决方法，以飨同侪。

## 环境

装isaaclab时可能网速会比较慢，国内镜像源也没有，可以尝试几个比较大的包单独下载再pip install。

## URDF导出

使用solidworks中的sw_urdf_export插件，选择“静应力仿真.sldrt”装配体文件进行导出，装配体文件略有修改，详情见我发的文件。  
  
坐标系、旋转轴、joint与link的定义命名规范参考unitree_go2的官方urdf。  
  
导出方法参考<https://blog.csdn.net/qq_54900679/article/details/137279115>以及<https://zhuanlan.zhihu.com/p/713198172>，具体步骤包括：  
1、安装插件，打开装配体文件；  
2、定义关节原点、坐标系、旋转轴，选择参考几何体，参考go2添加即可；  
3、重整装配体，解散原有的子装配体关系，按照base_link--hip--thigh--calf--foot重新组装子装配体，组装时给每个link选择相应的原点；  
4、参数覆盖，选择评估-质量属性-挨个选择子装配体-覆盖，软件会给每个link子装配体自动配置好质心、质量、转动惯量参数，这样之后导出urdf时插件会自动填好这些参数。显而易见，这样的精度是比较低的，后续应当每个零件手动测量标定；  
5、参考电机规格书确定电机参数；  
6、使用插件进行导出即可，正确设置各link的父子关系，配置各joint的坐标系与旋转轴，hip、thigh和calf关节选择revolute类型，wheel关节选择continous类型，foot关节选择fixed类型。fixed关节不需要坐标系和旋转轴，如果urdf自动配置了，记得删掉。最后配置好电机参数，检查无误后导出保存即可。

## 框架搭建

本项目基于<https://github.com/yusongmin1/My_unitree_go2_gym>进行迁移，整体框架从isaacgym换为issaclab，复用My_unitree_go2_gym的训练配置。并新增了openrobot的训练任务，以go2的环境为基类继承。目前仅实现了trot步态训练任务。

## 调试默认姿态和关节限位

导入机器人进行仿真强化学习训练，要设定好默认关节角度、初始高度和关节限位。写了一个脚本<scripts/tools/tune_openrobot_default_pose.py>用来调试，可以参考unitree_go2官方训练仓库设置的默认姿态，关节限位根据实际的机械限位设置。  
  
默认角度和初始高度在<source/My_quadruped_robot_lab/My_quadruped_robot_lab/assets/openrobot.py>里修改，关节限位在<source/My_quadruped_robot_lab/My_quadruped_robot_lab/tasks/manager_based/quadruped/trot/openrobot_env_cfg.py>里修改。  
  
目前的设置没什么问题。

## 针对openrobot训练调参

用原项目的训练配置训go2没问题，但直接照搬过来训openrobot得到结果是模型只会站在原地一动不动。原因是速度跟踪的奖励太少，模型只学会了去保持站立不动去拿那些防止倒地一类的奖励。解决方法是调高track_velocity的weights，并新增了penalty_when_commanded的惩罚，惩罚机器人在收到速度指令时（velocity_command模长大于阈值）原地不动（机器人实际速度小于阈值）。  
  
目前的配置训出来的模型效果还行。  
  
调参时可以打开tensorboard观察reward和loss的曲线，一般看个两三千轮就知道调的怎么样了。